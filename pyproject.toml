[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mylist"
version = "1.0.0"
description = "My custom list implementation in C"
readme = "README.md"
authors = [
    {name = "Ivan Korolev", email = "Ivankorol1512@yandex.ru"}
]
license = {text = "MIT"}
requires-python = ">=3.12"
dependencies = []

[project.urls]
Homepage = "https://github.com/KorolevIvanMi"
Repository = "https://github.com/KorolevIvanMi/C_lib"

[tool.setuptools]
# Явно говорим: никаких пакетов не искать
packages = []